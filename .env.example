# Локально: brew postgresql@16 (порт 5432, доверенная авторизация)
DATABASE_URL=postgresql+psycopg:///flowlens

# Вариант с docker-compose из репозитория:
# DATABASE_URL=postgresql+psycopg://flowlens:flowlens@localhost:5433/flowlens
